# Our Little Family

A cozy, responsive pixel-art family website made with plain HTML, CSS, and JavaScript.

## Preview locally

Open `index.html` directly, or serve this folder with any small static web server.

## Publish with GitHub Pages

Push the contents of this folder to a GitHub repository, then choose the `main` branch and repository root as the GitHub Pages publishing source.
